# Environment configuration for IDX list app
TZ=Asia/Jakarta
STORAGE=sqlite
DB_PATH=data/idx_quotes.db
TICKERS_PATH=config/tickers.json
RSI_MIN=55
HIGH_LOOKBACK=30
HIGH_WITHIN_DAYS=5
FETCH_INTERVAL_MIN=60
ENABLE_SCHEDULER=false
CSV_DIR=data
